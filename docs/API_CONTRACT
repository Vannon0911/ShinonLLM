export function APICONTRACTMain(input: {
  implementedContractsAndBehavior: string;
}): {
  exactOperationalSpecGuidance: string;
} {
  return {
    exactOperationalSpecGuidance:
      [
        "Scope: chat, health, and fail-closed error responses only.",
        "Input: implemented contracts and behavior from backend/src/routes/chat.ts and backend/src/routes/health.ts.",
        "Output: exact operational/spec guidance for the API contract with no ambiguity.",
        "Rules: preserve required symbols, keep deterministic behavior, and reject unclear normative language.",
      ].join("\n"),
  };
}

APICONTRACTMain

Input: implemented contracts and behavior
Output: exact operational/spec guidance
Error: no ambiguous normative language
